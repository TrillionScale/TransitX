{
  "name": "@transitx/xrpl-core",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "_comment": "raw TS 패키지 — Metro(모바일)는 babel로 트랜스파일, 스크립트(Node)는 tsx로 실행. xrpl은 peerDependency: 모바일은 자기 node_modules의 xrpl(폴리필 적용된)을 쓰고, 스크립트는 루트 node_modules의 xrpl을 쓴다.",
  "peerDependencies": {
    "xrpl": ">=4"
  }
}
